// Bit Finite Primitives Test Support

internal import Bit_Finite_Primitives

// Bit's `Finite.Enumerable` witness (count / ordinal / init(_unchecked:ordinal:))
// is surfaced via `import Bit_Finite_Primitives`; tests use the `.zero` / `.one`
// (Bit) and `.msb` / `.lsb` (Bit.Order) cases directly.
